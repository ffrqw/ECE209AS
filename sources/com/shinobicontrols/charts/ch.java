package com.shinobicontrols.charts;

import android.graphics.drawable.GradientDrawable;

class ch extends GradientDrawable {
    ch(int i, int i2, float f) {
        setShape(0);
        setColor(i);
        setStroke(ca.c(f, 1.0f), i2);
    }
}
